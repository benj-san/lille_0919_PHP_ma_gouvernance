{% extends 'base.html.twig' %}

{% block main %}
<p>board {{ board.id }}</p>
<p> date{{ board.dateStart }}</p>
    {% for advisor in board.advisors  %}
        <div class="cardAdvisors">
            <img src="https://www.laravoire.immo/wp-content/uploads/2017/06/Business-Woman-papers.jpg" alt=""/>
            <h4 class="cardTitle">{{ advisor.firstname }} {{ advisor.name }}</h4>
            <p>{{ advisor.place }}</p>
        </div>
    {% endfor %}



{% endblock %}

{% block title %}

{% endblock %}

